name: Web Cache Poisoning Hunter

on:
  workflow_dispatch:
    inputs:
      domains:
        description: 'Domains separated by / (e.g., f.com/j.com/x.com)'
        required: true
        type: string
      file_path:
        description: 'Path to a domains .txt file in repo (one per line)'
        required: false
        type: string

jobs:
  cache-poison-hunter:
    runs-on: ubuntu-latest
    env:
      TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
      TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
    
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Install Dependencies
        run: |
          sudo apt-get update > /dev/null 2>&1 && sudo apt-get install -y jq > /dev/null 2>&1

      - name: Install Tools
        run: |
          go install github.com/projectdiscovery/subfinder/v2/cmd/subfinder@latest > /dev/null 2>&1
          go install github.com/projectdiscovery/httpx/cmd/httpx@latest > /dev/null 2>&1
          go install github.com/projectdiscovery/katana/cmd/katana@latest > /dev/null 2>&1
          go install github.com/projectdiscovery/nuclei/v3/cmd/nuclei@latest > /dev/null 2>&1
          echo "$HOME/go/bin" >> $GITHUB_PATH

      - name: Update Nuclei Templates
        run: nuclei -ut > /dev/null 2>&1

      - name: Prepare Targets
        run: |
          touch targets.txt
          if [ -n "${{ github.event.inputs.domains }}" ]; then
            echo "${{ github.event.inputs.domains }}" | tr '/' '\n' | tr -d '\r' | sed 's/^[[:space:]]*//;s/[[:space:]]*$//' | grep -v '^$' >> targets.txt
          fi
          if [ -n "${{ github.event.inputs.file_path }}" ] && [ -f "${{ github.event.inputs.file_path }}" ]; then
            cat "${{ github.event.inputs.file_path }}" | sed 's/^[[:space:]]*//;s/[[:space:]]*$//' | grep -v '^$' >> targets.txt
          fi
          sort -u targets.txt -o targets.txt
          
          curl -s -X POST "https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/sendMessage" \
            -d chat_id="${TELEGRAM_CHAT_ID}" \
            --data-urlencode "text=🧪 Cache Poisoning Pipeline Starting for $(wc -l < targets.txt | xargs) targets..." || true

      - name: Step 1 - Subfinder & Tech Detect
        run: |
          set +e
          while IFS= read -r target; do
            if [ -z "$target" ]; then continue; fi
            safe=$(echo "$target" | sed 's/[^a-zA-Z0-9]/_/g' | cut -c 1-50)
            
            subfinder -d "$target" -silent -o "${safe}_subs.txt" || true
            touch "${safe}_subs.txt"
            
            cat "${safe}_subs.txt" | httpx -tech-detect -json -silent -o "${safe}_httpx.json" || true
            jq -r '.url' "${safe}_httpx.json" 2>/dev/null | sort -u > "${safe}_live.txt" || true
            touch "${safe}_live.txt"
            
          done < targets.txt
          
          cat *_live.txt > all_live.txt 2>/dev/null || true
          cat *_httpx.json > all_httpx.json 2>/dev/null || true
          touch all_live.txt all_httpx.json

      - name: Step 2 - Katana Deep Crawl
        run: |
          if [ ! -s "all_live.txt" ]; then exit 0; fi
          
          cat all_live.txt | katana -d 3 -jc -aff -silent -o katana_raw.txt || true
          cat katana_raw.txt | httpx -fc 404 -silent | sort -u > all_endpoints.txt || true
          touch all_endpoints.txt
          
          curl -s -X POST "https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/sendDocument" \
            -F chat_id="${TELEGRAM_CHAT_ID}" \
            -F document="@all_endpoints.txt" \
            -F caption="🕷️ Crawled Live Endpoints to Test for Cache Poisoning" || true

      - name: Step 3 - Isolate Cached Infrastructure
        run: |
          if [ ! -s "all_httpx.json" ]; then exit 0; fi
          
          > cached_targets.txt
          jq -r 'select(.tech | join(" ") | test("cloudflare|fastly|varnish|akamai|nginx|squid|cdn"; "i")) | .url' all_httpx.json >> cached_targets.txt 2>/dev/null || true
          
          for url in $(cat cached_targets.txt); do
            domain=$(echo "$url" | awk -F/ '{print $3}')
            grep "$domain" all_endpoints.txt >> cached_endpoints.txt 2>/dev/null || true
          done
          
          if [ ! -s "cached_endpoints.txt" ]; then
            cp all_endpoints.txt cached_endpoints.txt
          fi
          sort -u cached_endpoints.txt -o cached_endpoints.txt
          touch cached_endpoints.txt
          
          curl -s -X POST "https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/sendDocument" \
            -F chat_id="${TELEGRAM_CHAT_ID}" \
            -F document="@cached_endpoints.txt" \
            -F caption="☁️ Endpoints Sitting Behind Caching Infrastructure" || true

      - name: Step 4 - Pro Header Reflection Fuzzing
        run: |
          if [ ! -s "cached_endpoints.txt" ]; then exit 0; fi
          
          > reflection_hits.txt
          head -n 50 cached_endpoints.txt > fuzz_sample.txt
          
          cat fuzz_sample.txt | xargs -P 10 -I {} curl -s --max-time 5 "{}" \
            -H "X-Forwarded-Host: evil-cacher-poison.com" \
            -H "X-Host: evil-cacher-poison.com" \
            -H "X-Forwarded-Scheme: https" \
            -H "X-Cache-Key: evil-cacher-poison.com" 2>/dev/null | grep -i "evil-cacher-poison.com" >> reflection_hits.txt || true
          
          if [ -s "reflection_hits.txt" ]; then
            curl -s -X POST "https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/sendDocument" \
              -F chat_id="${TELEGRAM_CHAT_ID}" \
              -F document="@reflection_hits.txt" \
              -F caption="🚨 CRITICAL: Unkeyed Header Reflection Detected!" || true
          else
            curl -s -X POST "https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/sendMessage" \
              -d chat_id="${TELEGRAM_CHAT_ID}" \
              --data-urlencode "text=✅ No immediate unkeyed header reflection found." || true
          fi

      - name: Step 5 - Nuclei Cache Poisoning Engine
        run: |
          if [ ! -s "cached_endpoints.txt" ]; then exit 0; fi
          
          nuclei -l cached_endpoints.txt -tags cache-poison,cache-deception -silent -o nuclei_cache_results.txt || true
          touch nuclei_cache_results.txt
          
          if [ -s "nuclei_cache_results.txt" ]; then
            curl -s -X POST "https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/sendDocument" \
              -F chat_id="${TELEGRAM_CHAT_ID}" \
              -F document="@nuclei_cache_results.txt" \
              -F caption="🔥 CONFIRMED CACHE POISONING VULNERABILITIES!" || true
          else
            curl -s -X POST "https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/sendMessage" \
              -d chat_id="${TELEGRAM_CHAT_ID}" \
              --data-urlencode "text=✅ Nuclei: No automated cache poisoning vectors confirmed." || true
          fi

      - name: Upload All Artifacts
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: cache-poisoning-run-${{ github.run_id }}
          path: |
            *.txt
            *.json
          retention-days: 7

      - name: Pipeline Finished
        if: always()
        run: |
          curl -s -X POST "https://api.telegram.org/bot${TELEGRAM_BOT_TOKEN}/sendMessage" \
            -d chat_id="${TELEGRAM_CHAT_ID}" \
            --data-urlencode "text=🛑 Web Cache Poisoning Pipeline Completely Finished." || true
